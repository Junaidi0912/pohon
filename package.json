{
  "name": "pohon",
  "version": "1.0.0",
  "description": "Sistem Gamifikasi dengan Konsep Pohon Pengetahuan",
  "main": "server.js",
  "scripts": {
    "server": "json-server --watch db.json --port 3000 --host 0.0.0.0",
    "start": "json-server --watch db.json --port 3000 --host 0.0.0.0",
    "dev": "json-server --watch db.json --port 3000"
  },
  "keywords": ["pohon", "education", "siswa", "gamification"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}